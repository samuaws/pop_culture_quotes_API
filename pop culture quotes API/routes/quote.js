// const express = require("express"),
// router = express.Router();

// router.route("/").post(createQuote);
// router.route("/:id").get(showQuote).put(editQuote).delete(deleteQuote);

// module.exports = router;
